

<%if @current_user%>
<%@likecomment=Likecomment.find_by(user_id: @current_user.id, comment_id: comment.id)%>
<%if @likecomment%>
<object><%= link_to("/posts/#{post.id}/comments/#{comment.id}/likecomments/#{@likecomment.id}",method: :delete,remote: true) do %>
  <span class="fa fa-heart like-btn-unlike fa-xs"><%=Likecomment.where(comment_id:comment.id).count%></span>
<% end %></object>
<% else %>
<object><%= link_to("/posts/#{post.id}/comments/#{comment.id}/likecomments", method: :post,remote: true) do %>
  <span class="fa fa-heart like-btn fa-xs"><%=Likecomment.where(comment_id:comment.id).count%></span>
<% end %></object>
<% end %>

<%else%>
<object><%= link_to("/likecomments/#{comment.id}/destroy",method: :post,remote: true) do %>
  <span class="fa fa-heart like-btn-unlike fa-xs"><%=Likecomment.where(comment_id:comment.id).count%></span>
<% end %></object>
<%end%>